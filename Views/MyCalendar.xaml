<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="MinistryTracker.Views.MyCalendarPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="My Calendar">

    <Grid RowDefinitions="Auto,*" Padding="16" RowSpacing="12">

        <!-- Header / quick actions -->
        <HorizontalStackLayout Spacing="12">
            <Label
                Text="Scheduled visits"
                FontSize="20"
                FontAttributes="Bold"
                VerticalOptions="Center" />

            <ActivityIndicator
                IsRunning="{Binding IsBusy}"
                IsVisible="{Binding IsBusy}"
                VerticalOptions="Center" />
        </HorizontalStackLayout>

        <!-- List -->
        <CollectionView
            Grid.Row="1"
            ItemsSource="{Binding Items}"
            SelectionMode="None"
            EmptyView="No scheduled visits yet. Your future self is both relieved and suspicious.">

            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="12" RowDefinitions="Auto,Auto" ColumnDefinitions="*,Auto">
                        <Label
                            Text="{Binding StudentName}"
                            FontAttributes="Bold"
                            LineBreakMode="TailTruncation" />

                        <Label
                            Grid.Row="1"
                            Text="{Binding WhenDisplay}"
                            Opacity="0.75" />

                        <Button
                            Grid.Column="1"
                            Text="Open"
                            Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.OpenVisitCommand}"
                            CommandParameter="{Binding .}" />
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

    </Grid>
</ContentPage>
